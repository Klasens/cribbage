// FILE: server/game/scoring/hand.js
// Hand scoring placeholder. We'll implement real cribbage scoring later.

function scoreHand(cards = [], starter = null, isCrib = false) {
  // TODO: implement 15s, pairs, runs, flush, knobs.
  return 0;
}

module.exports = { scoreHand };
